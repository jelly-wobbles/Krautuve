{% trans_default_domain 'FOSUserBundle' %}

<div class="main-container col-lg-offset-4 col-lg-4 col-md-offset-1 col-md-10">
<form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit" novalidate>
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-lg btn-primary" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
</form>
    </div>
